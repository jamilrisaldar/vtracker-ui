# Google OAuth 2.0 Web client ID (Google Cloud Console → APIs & Services → Credentials).
# Used by @react-oauth/google for the Sign in with Google button.
# Leave unset to use only the simulated Google sign-in on the login page (mock API).
VITE_GOOGLE_CLIENT_ID=
